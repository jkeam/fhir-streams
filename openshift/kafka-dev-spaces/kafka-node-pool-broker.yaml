apiVersion: kafka.strimzi.io/v1
kind: KafkaNodePool
metadata:
  name: fhir-broker
  labels:
    strimzi.io/cluster: fhir-cluster
spec:
  replicas: 3
  roles:
    - broker
  storage:
    type: jbod
    volumes:
      - id: 0
        type: ephemeral
