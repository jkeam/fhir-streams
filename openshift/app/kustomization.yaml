apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: fhir

resources:
- deployment.yaml
- service.yaml
- route.yaml

commonLabels:
  app.kubernetes.io/part-of: fhir-streams
  app.kubernetes.io/managed-by: kustomize
